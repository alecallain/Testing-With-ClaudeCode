Run the following checks in order and report the results:

1. **Type-check & Build** — run `npm run build` and report any TypeScript or build errors.
2. **Unit Tests** — run `npx vitest run` and report how many tests passed, failed, or were skipped.

If any step fails, show the relevant error output and stop. If all steps pass, confirm that the build succeeded and all tests are green.
